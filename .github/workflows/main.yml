name: Test Action  # Defines the workflow name

on:
  push:
    branches:
      - main  # Runs when changes are pushed to the main branch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3  # Official GitHub action to get the repository code

      - name: 22f3001090@ds.study.iitm.ac.in  # Your email in the step name
        run: echo "Hello, world!"  # Runs a simple command
